---
title: "ECDSA Signer"
---

To create an account owned by a single EOA:

```ts
const rhinestoneAccount = await createRhinestoneAccount({
  owners: {
    type: 'ecdsa',
    accounts: [account],
  }
  rhinestoneApiKey,
})
```
