---
title: "Sudo Policy"
---

Sudo policy allows any action to pass. It is the default session.

To enable a sudo policy:

```ts {6-10}
const session: Session = {
  owners: {
    type: 'ecdsa',
    accounts: [sessionOwnerAccount],
  },
  policies: [
    {
      type: 'sudo',
    },
  ],
}
```
